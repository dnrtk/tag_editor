# 用途
画像ファイルのタグを編集するツール

# 機能
## 基本機能
- ファイルをドラッグアンドドロップして開く
- 対象のファイルを表示し、ウィンドウの右サイドバーにタグを表示する
- 左キーで同じフォルダ内の1つ前のに移動、右キーで1つ次のファイルに移動
- 右サイドバーでタグを参照/編集できる
- 左サイドバー・右サイドバーはメインウィンドウへのドッキングとフローティング(独立OSウィンドウ)を切り替え可能
  - デフォルトはドッキング
  - 各サイドバーのヘッダにある切替ボタンでドッキング/フローティングを切り替え可能
  - ドッキング/フローティングの状態とサイズは設定として永続化
- 左サイドバーは表示/非表示の切り替え可能 (Ctrl+F)
- 右サイドバーは表示/非表示の切り替え可能 (Ctrl+T)
- 左サイドバーのファイル名は折り返さず、表示枠を超える場合は末尾を ... で省略表示
  - 表示枠は手動で広げ可能(ドッキング時はドラッグで横幅変更、フローティング時はOSウィンドウのリサイズ)
- ctrl+sでタグをファイルに保存する
- 0～9に表示中ファイルに対してタグの追加/削除を行うホットキーを設定可能
- 0～9に該当するタグが付与されている場合は表示中画像の上部にオーバーレイ表示
- 0～9でタグを更新する場合はオートセーブのON/OFF切り替えが可能
- 左サイドバーにはファイルツリーを表示し、フォルダはopen/closeの切り替えが可能
- Escキーで現在表示中の画像を閉じる(ファイル未選択状態に戻る)
- テキスト入力欄にフォーカスがある間は単一キーショートカット(矢印, Esc, Delete, ホットキータグ)は無効化される(Ctrl 系の修飾キー付きショートカットは引き続き有効)

## スライドショー機能
- スライドショー開始ダイアログで対象ファイルを絞り込む
  - メインウィンドウとは独立したフローティングウィンドウとして表示され、メインウィンドウ外にも移動可能
  - サイズと位置は次回起動時にも保持
  - 右サイドバーに登録されているホットキータグを複数選択可能(チップ形式)
  - フリーワード入力欄でタグの部分一致を指定可能
  - ホットキータグ選択とフリーワードは同時指定可能(両方を満たす画像が対象)
  - 選択中の条件に合致するファイル一覧をリアルタイムにフィルタリング表示
  - ファイル一覧表示は2モード切り替え可能
    - リスト表示: ファイル名のみ
    - サムネイル表示: 小さいプレビュー画像のみ(ファイル名は表示せず、ホバー時にツールチップで表示)
- 画像の切り替わり時間は設定可能
- スライドショーが1巡終わったら最初から再開するか、終了するかの設定も可能

## 検索機能
- 検索開始ダイアログでベースフォルダを指定し、そこから再帰的にサブフォルダ全体の画像を対象に検索する
  - メインウィンドウとは独立したフローティングウィンドウとして表示され、サイズは次回起動時にも保持
  - ベースフォルダはフォルダ選択ダイアログで指定（メニューから開いた際は現在のファイルツリーのルートを初期値とする）
  - 「Search」操作でベースフォルダ配下を再帰走査し、タグ対応画像のタグを一括読込
  - スライドショーと同様、ホットキータグの複数選択（チップ形式）とフリーワードの部分一致を同時指定可能（両方を満たす画像が対象）
  - チップ・フリーワードの変更時はリアルタイムに絞り込みを再計算（再走査は不要）
  - 結果一覧はリスト表示（ベースフォルダからの相対パス）/サムネイル表示を切り替え可能
- フィルタリングした結果のファイルを一括でエクスポート可能
  - エクスポートは出力先フォルダへのコピー（非破壊、元ファイルは残る）
  - ベースフォルダからの相対パス（サブフォルダ構成）を維持して出力するため、別サブフォルダの同名ファイルが衝突しない
- Web クライアントでも同等の機能を提供
  - メインUI上に重ねて表示されるドラッグ可能なフローティングパネルとして開閉（位置はブラウザ側に永続化）
  - ベースフォルダ・出力先フォルダはパス文字列で指定し、サーバ側ファイルシステム上でコピーを実行

## Webサーバ機能
- アプリ起動時に内蔵Webサーバが起動し、ブラウザから同等の機能にアクセス可能
  - 待ち受けるホスト/インターフェースは設定ファイルの `web_host` で指定可能（デフォルト `0.0.0.0`）
    - `0.0.0.0` で同一LAN内の他ホストからもアクセス可能、`127.0.0.1` でローカルマシンに限定
  - デフォルトポートは 47823 (80 / 8080 を避けた高位ポート)
  - ポート番号は Settings メニューから変更可能(変更時は古いリスナーを停止し新ポートで再起動)
  - Settings メニューでサーバの有効/無効も切替可能
- メニューバー右端の「🌐 Open in Browser」ボタンから既定のブラウザで Web UI を開ける
- Webクライアントが提供する機能
  - ディレクトリ・画像のブラウズ
  - 画像の表示
  - タグの参照・追加・削除・保存
  - ホットキータグからのワンクリックでのタグ付与
  - 画像送り/戻し (←/→)
  - 画像非表示状態への復帰 (Esc)
  - スライドショーのサムネイル表示モードはプレビュー画像のみで構成し、ファイル名はホバー時のツールチップで表示
  - 左サイドバーのファイル名は折り返さず ... で省略表示。左サイドバーと中央ペインの間のスプリッタを左右ドラッグして横幅を調整可能(調整値はブラウザ側に永続化)
  - スライドショー開始設定はメインUI上に重ねて表示されるドラッグ可能なフローティングパネルとして開閉(位置はブラウザ側に永続化)。タグ・フリーワードでのフィルタリングを含む
  - 検索機能(再帰検索・タグフィルタ・結果の一括エクスポート)も同様のフローティングパネルとして提供
  - 静的アセット (HTML/CSS/JS) には `Cache-Control: no-store` を付与し、サーバ側更新をブラウザが即座に反映
- Webクライアントとデスクトップクライアントは独立したセッションとして動作するが、ファイルシステム上のタグは即座に共有される

## 共有フォルダ / アクセス制御
- アクセス可能なフォルダを設定ファイル（`shared_folders`）に事前登録できる
  - ユーザ設定ファイルおよび実行ファイル同梱の `settings.json` のどちらでも指定可能（`[{ "name": 表示名, "path": フォルダパス }]`。`name` 省略時は末尾のフォルダ名）
- 登録フォルダが1件以上ある場合、Webサーバ経由の全ファイルアクセス（ツリー閲覧・画像表示・サムネ・タグ参照/保存・フィルタ・再帰検索・エクスポート）を**登録フォルダおよびその配下に限定**する
  - 他ホストから登録外のパス（例: `C:\Windows`）は参照できない（403）。`..` やシンボリックリンクによる脱出も `canonicalize` で防止
  - エクスポートのコピー先も登録フォルダ配下に限定（書き込みの逸脱防止）
  - 登録が0件の場合は制限なし（単一PCのローカル利用向けの従来動作）
- 登録フォルダはワンクリックで開ける
  - Web UI: 左サイドバー「Shared folders」一覧から選択。パス未指定時の初期表示も先頭の登録フォルダ
  - デスクトップ: File メニュー「Open Shared Folder」から選択
- 他ホストからアクセスする場合、OS のファイアウォールで当該ポートの受信許可が別途必要

## ヘッドレス（サーバ専用）ビルド
- GUI を含まないサーバ専用ビルドを Cargo feature で切り替え可能
  - デフォルトは GUI 付き（`default = ["gui"]`）。`cargo build --no-default-features` で eframe/winit/GTK 等の GUI 依存を除いた pure-Rust のサーバのみをビルド
  - サーバ専用ビルドは Web サーバを起動して常駐（GUI 無し）。GUI 付きビルドでも `--server` 引数で同じヘッドレス起動が可能
  - 依存は pure Rust のみ（C ライブラリ非依存）のため、ARM 等へのクロスコンパイル/コンテナ化が容易
- Raspberry Pi / NAS（OpenMediaVault 等）向けに Docker イメージを提供（`Dockerfile`、`deploy/` 配下に compose と設定例・手順）
